# MongoDB Atlas Connection String
# Get this from: cloud.mongodb.com -> Database -> Connect -> Drivers
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-url>.mongodb.net/pulsechat?retryWrites=true&w=majority

# Port for local dev server
PORT=3000
